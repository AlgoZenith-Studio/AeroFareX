# infra/docker

docker-compose.yml for local development: postgres (with TimescaleDB extension), plus any local
emulators (Firebase emulator suite) needed to develop without hitting live Firebase projects.
