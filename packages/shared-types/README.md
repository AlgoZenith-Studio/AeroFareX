# packages/shared-types

TypeScript types shared between apps/web and its mock layer: FareObservation, IndexSnapshot,
AttributionContribution, QualityMetadata, SourceHealth, etc. Kept in sync with apps/api Pydantic
schemas by convention (no codegen yet, worth revisiting once the API stabilizes).
