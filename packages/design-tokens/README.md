# packages/design-tokens

Single source of truth for tokens.css (TRD Part C) and the Tailwind preset that maps CSS custom
properties to Tailwind utility classes. apps/web consumes this package; no other package may
define color/spacing/radius values independently.
